package ${ROOT_PACKAGE}.feature.${FEATURE_NAME}.presentation.${SCREEN_PACKAGE}

import me.bookk.core.presentation.ViewModel
import me.bookk.core.presentation.VmArgs
import me.bookk.core.presentation.memory.weakSelfClosure
import me.bookk.feature.${FEATURE_NAME}.presentation.${FEATURE_NAME_CAPITAL}StateFactory

class ${SCREEN_NAME}ViewModel(
    stateFactory: ${FEATURE_NAME_CAPITAL}StateFactory,
    vmArgs: VmArgs
) : ViewModel(vmArgs) {

    val uiState: ${SCREEN_NAME}State = stateFactory.create${SCREEN_NAME}State().setup()

    private fun ${SCREEN_NAME}State.setup() = apply {
        // TODO: Initialize UI state
        // appBar.title = ...
        // appBar.onBackClick = weakSelfClosure { it.uiState.navigation.push(${SCREEN_NAME}Destination.Back) }
    }
}
