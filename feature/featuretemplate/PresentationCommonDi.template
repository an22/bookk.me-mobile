package ${ROOT_PACKAGE}.feature.${FEATURE_NAME}.presentation.di

import org.koin.core.module.Module
import org.koin.dsl.module

internal expect fun platform${FEATURE_NAME_CAPITAL}DiModule(): Module

fun ${FEATURE_NAME}PresentationModule() = module {
    includes(platform${FEATURE_NAME_CAPITAL}DiModule())
}