package me.bookk.designsystem.uistate

/**
 * UIState implementation can ba found in the corresponding package of ios project,
 * ui state is controlled by SwiftUI so we can't access it from KMM (yet)
 */
